# frozen_string_literal: true

RSpec.describe Katalyst::Basic::Auth do
  it "has a version number" do
    expect(Katalyst::Basic::Auth::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
